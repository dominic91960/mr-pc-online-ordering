function redirect() {
    window.location.href = '/components/pcComponents.php';
}

/*function updateCart(id, order_quantity){
    document.cookie = "item_id = "+id;
    document.cookie = "order_quantity = "+order_quantity;

    $('#order_button').on('click', function() {
        if (window.confirm("Are you sure?")) {
            alert("Order placed successfully!");
        }
    });
}*/